{
  "name": "polysync-engine",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "PolySync's deterministic engine: plans, hybrid scheduling rules H1-H9, bounds checker B1-B9, contraindications and the exercise library. Pure TypeScript with no runtime dependencies.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "eval": "tsx ../evals/run.ts && tsx ../evals/hybrid-run.ts"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "tsx": "^4.23.15",
    "typescript": "~5.9.3",
    "vitest": "^5.0.1"
  }
}
